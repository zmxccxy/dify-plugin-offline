# Dify 插件离线包打包文档（适配 Dify 1.17.0）

本目录提供一套跨平台工具，把一个**现成的 Dify 插件 `.difypkg`** 重打成**内网离线安装包**：
依赖 wheel 全部内置进包，服务器安装时**完全不需要联网**。

- 脚本：`build-offline-pkg.py`（macOS / Windows / Linux 通用，仅需 Python 3.8+）
- 日志：控制台 + 脚本同目录 `build-offline-pkg.log`
- 参考官方文档：[Dify Plugin CLI](https://docs.dify.ai/en/develop-plugin/getting-started/cli)、
  [打包为本地文件并分享](https://enterprise-docs.dify.ai/zh/3.12.x/develop/plugins/publishing/marketplace-listing/release-by-file)、
  [dify-plugin-daemon Releases](https://github.com/langgenius/dify-plugin-daemon/releases)

---

## 1. 为什么需要重打包（原理）

Dify 1.17.0 的插件守护进程（plugin_daemon 0.6.x）安装插件时，会用 uv 给插件建独立 Python 环境并安装依赖：

- 包里同时有 `pyproject.toml`（+`uv.lock`）→ 执行 `uv sync --frozen`，**联网**解析；
- 只有 `requirements.txt` → 执行 `uv pip install -r requirements.txt`，同样默认**联网**。

离线包做的三件事（本脚本自动完成）：

1. 用 pip 把 `requirements.txt` 的全部依赖下载成**目标架构（arm64/aarch64 或 amd64/x86_64）的 wheel**，放进包内 `deps/` 目录；
2. 重写 `requirements.txt`，让每行指向 `./deps/xxx.whl`（双架构包用 `platform_machine` 标记区分）；
3. **删除 `pyproject.toml` 和 `uv.lock`**，强制守护进程走 `requirements.txt` 的本地安装路径。

wheel 平台标签取 `manylinux2014 + manylinux_2_28`、Python 版本取 `manifest.yaml` 的
`meta.runner.version`，与官方 plugin_daemon 镜像（Ubuntu 24.04）完全兼容。

---

## 2. 前置条件

| 项目 | 要求 |
| ---- | ---- |
| Python | 3.8+（用哪个 Python 跑脚本，就用哪个 Python 的 pip 下载 wheel） |
| 网络 | 打包机**能访问所选 pip 源**（装到内网服务器后不再需要网络） |
| 原始包 | 一个有效的 `.difypkg`，且**包含 `requirements.txt`**（官方市场/GitHub Releases 的包都满足） |

> ⚠️ 注意：脚本只负责“重打包”，不负责“从源码打包”。如果手里是源码目录，请先按官方文档
> [Dify Plugin CLI](https://docs.dify.ai/en/develop-plugin/getting-started/cli)
> 安装 CLI 并执行 `dify plugin package <插件目录> -o xxx.difypkg` 生成原始包。

### 官方 CLI 安装方式（仅从源码打包时才需要）

- **macOS**：`brew tap langgenius/dify && brew install dify`
- **Linux**：从 [dify-plugin-daemon Releases](https://github.com/langgenius/dify-plugin-daemon/releases) 下载
  `dify-plugin-linux-amd64` 或 `dify-plugin-linux-arm64`，`chmod +x` 后放入 PATH；
- **Windows**：下载 `dify-plugin-windows-amd64.exe`（或 `-arm64.exe`），改名为 `dify.exe` 加入 PATH。

---

## 3. 脚本用法

把原始 `.difypkg` 放到**脚本同目录**（或任意位置用 `--input` 指定），然后：

```bash
# macOS / Linux
python3 build-offline-pkg.py --arch arm64
python3 build-offline-pkg.py --arch amd64 --pip-source tsinghua
python3 build-offline-pkg.py --arch both --pip-source aliyun

# Windows（cmd / PowerShell）
python build-offline-pkg.py --arch arm64
py -3 build-offline-pkg.py --arch amd64
```

不传 `--input` 时，脚本自动识别脚本同目录下**文件名不含 `-offline`** 的 `.difypkg`；
有多个或找不到时会报错并提示。

### 参数表

| 参数 | 取值 | 说明 |
| ---- | ---- | ---- |
| `--input` | 路径 | 原始 `.difypkg`；缺省自动识别同目录非 offline 包 |
| `--arch` | `arm64` / `amd64` / `both` | 目标服务器架构（默认 `arm64`）。`both` 双架构打一个包，体积约翻倍 |
| `--pip-source` | `official` / `aliyun` / `tsinghua` / `tencent` / `ustc` | 下载依赖的 pip 源（默认官方 `pypi.org`） |
| `--pip-index-url` | URL | 自定义 pip 源，优先级高于 `--pip-source`（如公司内部 Nexus/Artifactory） |
| `--python-version` | 如 `3.12` | 覆盖 wheel 的 Python 版本（默认读 manifest，取不到时用 3.12） |
| `--retries` | 整数 | 每个下载动作的重试次数（默认 3） |
| `--no-verify` | — | 跳过 uv 离线解析验证 |
| `--log-file` | 路径 | 日志文件（默认脚本同目录 `build-offline-pkg.log`） |
| `--verbose` | — | 控制台输出调试级日志（日志文件始终记录全部） |

### 输出

- `原文件名-<arch>-offline.difypkg`（`both` 时为 `-all-arch-offline.difypkg`）
  例：`openai_api_compatible-0.0.65-arm64.difypkg` → `openai_api_compatible-0.0.65-arm64-offline.difypkg`
- 同名 `.sha256` 校验文件。

---

## 4. pip 源与下载失败处理

| 名称 | 地址 |
| ---- | ---- |
| `official` | `https://pypi.org/simple` |
| `aliyun` | `https://mirrors.aliyun.com/pypi/simple/` |
| `tsinghua` | `https://pypi.tuna.tsinghua.edu.cn/simple` |
| `tencent` | `https://mirrors.cloud.tencent.com/pypi/simple` |
| `ustc` | `https://pypi.mirrors.ustc.edu.cn/simple/` |

- 先按整个 requirements 整包下载（快）；**失败后自动转入逐包下载**，每个依赖单独重试并记录日志，
  已下载的部分自动复用、不会重复下载；
- 某个依赖最终仍下载失败时：控制台和日志里都会输出 `[ERROR] 依赖下载失败: xxx`，
  最终汇总失败清单，脚本以**退出码 1** 结束，不会产出半成品包；
- 常见失败原因：镜像同步延迟（实例：`gevent 26.5.0` 曾在清华镜像缺失，官方源正常）→
  **换一个源重试**即可（`--pip-source` 或 `--pip-index-url` 指向内部 Nexus/Artifactory）。

日志样例：

```
2026-09-05 18:30:12 [INFO] [3/43] gevent==26.5.0 —— 第 1/3 次下载尝试
2026-09-05 18:30:15 [WARNING] [3/43] gevent==26.5.0 —— 失败(exit=1): ERROR: No matching distribution ...
2026-09-05 18:30:45 [ERROR] 依赖下载失败: gevent==26.5.0
```

---

## 5. 打包后的自动校验

如果打包机装有 `uv`，脚本会用与守护进程**完全相同的命令**做离线解析验证：

```text
uv pip install --dry-run --offline --target ... --python-platform aarch64-unknown-linux-gnu \
    --python-version 3.12 -r requirements.txt
```

出现 `离线解析验证通过` 即说明该包在服务器上可以纯离线装依赖。没装 uv 会跳过（可选安装：`pip install uv`）。

---

## 6. 内网服务器安装注意事项

1. **签名校验**：重打包会破坏原包的官方签名。若安装时报
   `plugin verification has been enabled ... bad signature`，在部署目录 `.env` 设置
   `FORCE_VERIFYING_SIGNATURE=false` 并重启 `plugin_daemon` 容器：
   ```bash
   docker compose up -d plugin_daemon
   ```
2. **上传大小限制**：Dify 1.17.0 的 api 容器默认 `PLUGIN_MAX_PACKAGE_SIZE=52428800`（50MB），
   一般够用；`both` 双架构包可能超过，可在 `.env` 调大并重启 `api`；
3. **前端 nginx**：若上传时报 `413 Request Entity Too Large`，把对应 nginx 的
   `client_max_body_size` 调到 > 包体积（如 `100m`）并 reload；
4. **架构匹配**：arm64 包只能装到 aarch64 服务器，amd64 包只能装到 x86_64 服务器，
   不确定就用 `--arch both`。

---

## 7. FAQ

**Q1：为什么不直接改依赖版本号换小版本？**
保持与官方包完全一致（wheel 版本 = 官方 requirements 锁定的版本），降低运行时风险。

**Q2：为什么 wheel 用 `manylinux2014 + manylinux_2_28` 两个标签？**
个别包（如 gevent 新版）只发布 `manylinux_2_28` 的 wheel；同时保留 2014 兼容老一点的内核。
官方 plugin_daemon 镜像基于 Ubuntu 24.04（glibc 2.39），两者都可运行。

**Q3：Windows 上能跑吗？**
可以。脚本只用 Python 标准库；wheel 的跨平台下载（`pip download --platform`）在 Windows 上同样可用。
如遇控制台中文乱码，把终端切到 UTF-8（Windows Terminal 默认即可）。

**Q4：换插件新版本怎么重打？**
拿到新版本的原始 `.difypkg` 放到脚本同目录，重新执行即可（会覆盖同名旧离线包）。

**Q5：公司有内部 pip 镜像，怎么用？**
`--pip-index-url http://你的镜像地址/pypi/simple`，wheel 会从内部源下载，版本与官方一致即可。
